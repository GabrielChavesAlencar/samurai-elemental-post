using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ativar_post : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        configuracoes.postpros = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
